# GitHub部署教程 📦

## 快速部署（推荐）

### 方式一：使用脚本自动部署

1. **初次设置**（只需执行一次）
   ```bash
   # 双击运行
   setup_github.bat
   ```
   按提示输入你的GitHub仓库地址即可。

2. **后续更新**
   ```bash
   # 双击运行
   deploy.bat
   ```
   每次修改后运行此脚本即可推送到GitHub。

### 方式二：手动部署

如果你更喜欢手动操作，按以下步骤进行：

## 详细步骤

### 1. 创建GitHub仓库

1. 登录 [GitHub](https://github.com)
2. 点击右上角 `+` → `New repository`
3. 填写信息：
   - **Repository name**: `mobile-ai-assistant` (或其他名称)
   - **Description**: "多模型AI对比助手"
   - **Public** (公开仓库才能使用GitHub Pages)
   - 不要勾选 "Initialize this repository with a README"
4. 点击 `Create repository`

### 2. 推送代码到GitHub

在项目目录打开命令行，执行：

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 创建首次提交
git commit -m "🎉 初始化多模型AI对比助手项目"

# 连接远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/mobile-ai-assistant.git

# 推送代码
git branch -M main
git push -u origin main
```

### 3. 启用GitHub Pages

1. 进入你的仓库页面
2. 点击 `Settings` (设置)
3. 在左侧菜单找到 `Pages`
4. 在 `Source` 下拉菜单选择 `main` 分支
5. 点击 `Save`
6. 等待1-2分钟

### 4. 访问你的应用

部署完成后，你的应用地址为：
```
https://你的用户名.github.io/mobile-ai-assistant/
```

例如：`https://zhangsan.github.io/mobile-ai-assistant/`

## 常见问题

### ❓ 推送失败怎么办？

**问题1：权限被拒绝**
```bash
# 配置GitHub认证
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 使用Personal Access Token
# 1. 在GitHub生成Token: Settings → Developer settings → Personal access tokens
# 2. 使用Token作为密码推送
```

**问题2：连接超时**
```bash
# 可能需要配置代理
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy https://127.0.0.1:7890
```

**问题3：仓库已存在**
```bash
# 删除现有远程仓库配置
git remote remove origin

# 重新添加
git remote add origin https://github.com/你的用户名/mobile-ai-assistant.git
```

### ❓ GitHub Pages显示404？

1. 确认仓库是 **Public** (公开)
2. 检查 Settings → Pages 是否正确配置
3. 等待几分钟，GitHub Pages部署需要时间
4. 确认分支名称是 `main` 而不是 `master`

### ❓ 更新代码后网站没变化？

```bash
# 清除浏览器缓存，或使用无痕模式访问
# 等待1-2分钟让GitHub Pages重新构建
```

### ❓ API密钥会泄露吗？

**不会！** 你的API密钥存储在：
- ✅ 用户浏览器的 localStorage
- ✅ 不会上传到GitHub（已添加到.gitignore）
- ✅ 不会被其他人看到

但请注意：
- ⚠️ 不要在公共电脑保存API密钥
- ⚠️ 不要把 `api key.txt` 文件上传到GitHub

## 后续更新流程

每次修改代码后：

```bash
# 方式1：使用脚本（推荐）
deploy.bat

# 方式2：手动执行
git add .
git commit -m "更新说明"
git push origin main
```

## 自定义域名（可选）

如果你有自己的域名：

1. 在域名DNS设置中添加CNAME记录：
   ```
   记录类型: CNAME
   主机记录: www (或其他子域名)
   记录值: 你的用户名.github.io
   ```

2. 在GitHub仓库的 `Settings → Pages → Custom domain` 填入你的域名

3. 等待DNS生效（可能需要几小时）

## 安全建议

1. ✅ 定期更新API密钥
2. ✅ 使用强密码保护GitHub账号
3. ✅ 启用GitHub两步验证
4. ✅ 不要分享你的API密钥
5. ✅ 定期检查API使用量和费用

## 获取帮助

遇到问题？
- 📖 查看 [GitHub Pages文档](https://docs.github.com/pages)
- 💬 在仓库提Issue
- 🔍 搜索相关错误信息

---

**祝你部署顺利！** 🚀✨

