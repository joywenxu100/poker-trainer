# GitHub Pages 部署教程 - 零基础版

## 🎯 目标：让训练APP可以通过网址访问

完成后你会得到：`https://你的用户名.github.io/poker-trainer/`

---

## 📝 准备工作（5分钟）

### **1. 注册GitHub账号**

1. 打开 https://github.com
2. 点击右上角 `Sign up`（注册）
3. 填写：
   - Email（邮箱）
   - Password（密码，至少15个字符）
   - Username（用户名，建议用英文）
4. 验证邮箱
5. 完成！

---

## 🚀 方法1：网页操作（最简单，推荐）

### **步骤1：创建仓库**

1. 登录GitHub后，点击右上角 `+` → `New repository`

2. 填写仓库信息：
   ```
   Repository name: poker-trainer
   Description: 德州扑克决策训练APP
   选择: ✅ Public (公开)
   勾选: ✅ Add a README file
   ```

3. 点击 `Create repository` （创建仓库）

### **步骤2：上传文件**

1. 进入刚创建的仓库页面

2. 点击 `Add file` → `Upload files`

3. 拖拽这4个文件到网页：
   - `poker_trainer.html`
   - `poker_trainer.js`
   - `manifest.json`
   - `sw.js`

4. 在底部 `Commit changes` 框里输入：`初始上传`

5. 点击 `Commit changes`（提交更改）

### **步骤3：启用GitHub Pages**

1. 点击仓库顶部的 `Settings`（设置）

2. 左侧菜单找到 `Pages`

3. 在 `Source` 下拉菜单选择：`main` 分支

4. 点击 `Save`（保存）

5. 等待1-2分钟，刷新页面

6. 看到绿色提示框：
   ```
   Your site is live at https://你的用户名.github.io/poker-trainer/
   ```

### **步骤4：访问APP**

1. 点击上面的链接

2. 在浏览器输入：
   ```
   https://你的用户名.github.io/poker-trainer/poker_trainer.html
   ```

3. 完成！🎉

4. 把这个网址分享到手机，浏览器打开后添加到主屏幕

---

## 🚀 方法2：使用Git命令（进阶）

### **前提：安装Git**

**Windows用户**：
1. 下载 https://git-scm.com/download/win
2. 双击安装，全部默认选项即可

**Mac用户**：
```bash
brew install git
```

### **部署步骤**

1. **打开命令行**（Windows打开 Git Bash）

2. **配置Git**（首次使用）
```bash
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的邮箱"
```

3. **创建本地仓库**
```bash
cd "D:\user\德州扑克"
git init
```

4. **添加文件**
```bash
git add poker_trainer.html
git add poker_trainer.js
git add manifest.json
git add sw.js
git add index.html
```

5. **提交**
```bash
git commit -m "初始提交：德州扑克训练APP"
```

6. **关联远程仓库**
```bash
git remote add origin https://github.com/你的用户名/poker-trainer.git
```

7. **推送到GitHub**
```bash
git branch -M main
git push -u origin main
```

8. **启用GitHub Pages**
   - 浏览器打开 `https://github.com/你的用户名/poker-trainer`
   - Settings → Pages
   - Source 选择 `main`
   - Save

9. **完成！**访问：
   ```
   https://你的用户名.github.io/poker-trainer/poker_trainer.html
   ```

---

## 🎯 方法3：一键自动部署脚本（最快）

### **Windows用户**

1. **双击运行** `package.bat`
   - 会自动打包所有文件到 `poker_trainer_package` 文件夹

2. **手动上传到GitHub**
   - 登录 https://github.com
   - 创建新仓库 `poker-trainer`
   - 上传 `poker_trainer_package` 里的所有文件
   - 启用 Pages

---

## 📱 方法4：使用GitHub Desktop（图形界面）

### **安装GitHub Desktop**

1. 下载：https://desktop.github.com
2. 安装并登录GitHub账号

### **部署步骤**

1. **创建新仓库**
   - 打开GitHub Desktop
   - File → New Repository
   - Name: `poker-trainer`
   - Local Path: 选择 `D:\user\德州扑克`
   - Create Repository

2. **添加文件**
   - 把4个核心文件复制到仓库文件夹
   - GitHub Desktop 会自动检测到新文件

3. **提交更改**
   - 左下角输入提交信息：`初始提交`
   - 点击 `Commit to main`

4. **发布到GitHub**
   - 点击顶部 `Publish repository`
   - 确保 `Keep this code private` **不勾选**
   - 点击 `Publish repository`

5. **启用Pages**
   - 点击顶部 `Repository` → `View on GitHub`
   - 浏览器打开后，Settings → Pages → Source 选择 `main` → Save

6. **完成！**

---

## 🌐 替代方案：Cloudflare Pages（更快）

### **为什么选Cloudflare？**
- ✅ 中国访问速度更快
- ✅ 操作更简单
- ✅ 不需要Git

### **部署步骤**

1. **注册Cloudflare**
   - 打开 https://dash.cloudflare.com/sign-up
   - 注册账号（免费）

2. **创建Pages项目**
   - 登录后，左侧选择 `Workers & Pages`
   - 点击 `Create application`
   - 选择 `Pages` → `Upload assets`

3. **上传文件**
   - 项目名称：`poker-trainer`
   - 拖拽4个核心文件
   - 点击 `Deploy site`

4. **完成！**
   - 获得网址：`https://poker-trainer.pages.dev`
   - 访问测试
   - 分享到手机

---

## 💡 最简单的临时方案

如果你只是想快速测试，不想折腾：

### **方案：本地文件直接分享**

1. **双击运行 `package.bat`**
   - 自动打包所有文件

2. **压缩文件夹**
   - 右键 `poker_trainer_package` → 发送到 → 压缩文件

3. **发送到手机**
   - 微信文件传输助手
   - QQ我的设备
   - 百度网盘/阿里云盘

4. **手机使用**
   - 解压文件
   - 浏览器打开 `index.html`
   - 添加到主屏幕

**优点**：
- ✅ 最简单，0技术门槛
- ✅ 1分钟搞定

**缺点**：
- ❌ 每次打开需要找文件
- ❌ 更换设备需要重新传输

---

## 🎓 推荐方案总结

| 方案 | 难度 | 时间 | 推荐度 |
|------|------|------|--------|
| **GitHub Pages网页操作** | ⭐ | 5分钟 | ⭐⭐⭐⭐⭐ |
| **Cloudflare Pages** | ⭐ | 3分钟 | ⭐⭐⭐⭐⭐ |
| **本地文件分享** | ⭐ | 1分钟 | ⭐⭐⭐ |
| **GitHub Desktop** | ⭐⭐ | 8分钟 | ⭐⭐⭐⭐ |
| **Git命令行** | ⭐⭐⭐ | 10分钟 | ⭐⭐⭐ |

---

## 🆘 遇到问题？

### **问题1：上传失败**
- 检查文件大小（应该很小，< 1MB）
- 检查网络连接
- 尝试刷新页面重试

### **问题2：Pages没有启用**
- 确保仓库是 Public（公开）
- 确保文件已经上传成功
- 等待2-3分钟后刷新

### **问题3：访问404错误**
- 检查文件名是否正确（注意大小写）
- 访问完整路径：`https://用户名.github.io/poker-trainer/poker_trainer.html`
- 等待几分钟，GitHub Pages需要构建时间

### **问题4：手机添加到主屏幕不工作**
- 确保用系统浏览器（Safari/Chrome）
- 不要用微信/QQ内置浏览器
- 首次打开需要联网

---

## 📞 需要帮助？

告诉我你遇到的具体问题，我会帮你解决！

或者直接用**本地文件分享方案**，最简单实用！

---

**建议**：新手先用 `package.bat` 打包，微信发到手机试试效果。
满意后再学习GitHub Pages部署，获得永久网址！🚀



